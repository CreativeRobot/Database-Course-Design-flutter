# 图书分类层级功能执行记录

## 阶段
- [in_progress] 1. 后端分类层级约束、树形接口与父分类筛选
- [pending] 2. Flutter 分类模型、后台树形管理和商城级联筛选
- [pending] 3. 格式化、定向测试、全量验证和功能提交

## 当前决策
- 使用现有 `category.parent_id` 自关联，不做数据库迁移。
- 限制为一级分类与二级分类，避免课设展示中出现难以说明的无限层级。
- 继续保留 `GET /api/categories` 平铺响应；新增 tree 响应用于后台展示。
- 一级分类筛选必须命中其自身及直属二级分类。

## 已知环境记录
| 事项 | 结果 | 处理 |
|---|---|---|
| Maven Wrapper | PowerShell 启动脚本失败 | 使用系统 `mvn.cmd` 并指定项目 `.m2` 缓存 |
| 后端全量测试 | 35 个测试中 3 个错误 | 当前 `target/surefire-reports` 访问被拒绝；尚未修改分类代码 |
| Flutter 全量测试 | 长时间无输出后中断 | 后续只运行定向测试并保留命令及输出作为证据 |
| 计划记录首写 | 路径重复拼接 `superpowers` | 已改为正确的 `docs/superpowers/plans` 路径 |
